spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: ${DEV_USER:quiz}
    url: jdbc:postgresql://${DEV_HOST:localhost}:5432/${DEV_DB:quizdb}
    password: ${DEV_PASSWORD:quiz123}
  jpa:
    hibernate:
      # Schema agora é gerenciado pelo Flyway (db/migration) — Hibernate só
      # valida se o mapeamento das entidades bate com o schema real.
      ddl-auto: validate